# QR Code Reader

Проект на C++ для детектирования и распознавания QR-кодов с использованием библиотеки OpenCV.

## Возможности:

- **Детектирование QR-кодов** на изображениях
- **Декодирование данных** из QR-кодов
- **Пакетная обработка** нескольких изображений
- **Визуализация результатов** с выделением QR-кодов
- **Предобработка изображений** для улучшения распознавания
- **Логирование** процесса работы
- **Экспорт результатов** в текстовые файлы и изображения

## 🛠️ Установка и сборка

### Предварительные требования

- **Ubuntu/Debian** (или другая Linux-система)
- **OpenCV 4.x**
- **CMake 3.16+**
- **C++17 компилятор**

### Установка зависимостей

```bash
sudo apt update
sudo apt install build-essential cmake git pkg-config
sudo apt install libopencv-dev

# Клонирование репозитория
git clone <repository-url>
cd qr_reader

# Сборка
mkdir build && cd build
cmake ..
make

# Запуск
./qr_reader
```

## Использование:
```c++
#include "io/image_loader.h"
#include "core/qr_detector.h"
#include "io/result_writer.h"

// Загрузка изображения
auto imageLoader = ImageLoader::loadFromFile("path/to/image.jpg");

// Инициализация детектора
QRDetector detector;
detector.setPreprocessingEnabled(true);

// Распознавание QR-кода
auto result = detector.detectFromImage(imageLoader.image);

// Сохранение результатов
ResultWriter::printToConsole(result);
ResultWriter::saveVisualization(result, "output.png");
```